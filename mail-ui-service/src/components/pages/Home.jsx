import React from 'react'
import {observer} from "mobx-react-lite"

const Home = observer(() => {
    return (
        <div className={"container"}>
            <h2>Dashboard</h2>
            Dashboard
        </div>
    );
});

export default Home
